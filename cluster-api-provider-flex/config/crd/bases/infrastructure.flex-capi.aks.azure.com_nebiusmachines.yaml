---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nebiusmachines.infrastructure.flex-capi.aks.azure.com
spec:
  group: infrastructure.flex-capi.aks.azure.com
  names:
    kind: NebiusMachine
    listKind: NebiusMachineList
    plural: nebiusmachines
    singular: nebiusmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.providerID
      name: ProviderID
      type: string
    - jsonPath: .status.instanceState
      name: State
      type: string
    - jsonPath: .status.initialization.provisioned
      name: Ready
      type: string
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: NebiusMachine is the Schema for the nebiusmachines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NebiusMachine
            properties:
              imageFamily:
                description: |-
                  imageFamily is the Nebius image family to use for this machine (e.g. "ubuntu24.04-driverless").
                  Defaults to infer from platform and preset if not specified.
                type: string
              osDiskSizeGibibytes:
                default: 100
                description: osDiskSizeGibibytes is the size of the OS disk in GiB.
                format: int32
                minimum: 1
                type: integer
              platform:
                description: platform is the Nebius platform to use for this machine
                  (e.g. "cpu-d3", "gpu-h200-sxm").
                type: string
              preset:
                description: preset is the Nebius preset to use for this machine (e.g.
                  "4vcpu-16gb", "1gpu-128vcpu-1600gb").
                type: string
              projectID:
                description: projectID is the Nebius project ID where the instance
                  should be created.
                type: string
              providerID:
                description: providerID is the unique identifier as specified by the
                  cloud provider.
                maxLength: 512
                minLength: 1
                type: string
              region:
                description: region is the Nebius region where the instance should
                  be created.
                type: string
              subnetID:
                description: subnetID is the Nebius subnet ID to attach the instance
                  to.
                type: string
            required:
            - platform
            - preset
            - projectID
            - region
            - subnetID
            type: object
          status:
            description: status defines the observed state of NebiusMachine
            properties:
              addresses:
                description: addresses contains the machine's associated addresses.
                items:
                  description: MachineAddress contains information for the machine's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      type: string
                    type:
                      description: type is the type of the machine address.
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: conditions represent the current state of the NebiusMachine
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureMessage:
                description: |-
                  failureMessage will be set in the event that there is a terminal problem
                  reconciling the Machine and will contain a more verbose string suitable for logging and human consumption.
                type: string
              failureReason:
                description: |-
                  failureReason will be set in the event that there is a terminal problem
                  reconciling the Machine and will contain a succinct value suitable for machine interpretation.
                type: string
              initialization:
                description: |-
                  initialization provides observations of the NebiusMachine initialization process.
                  NOTE: Fields in this struct are part of the Cluster API contract, and they are used to orchestrate initial Machine provisioning.
                properties:
                  provisioned:
                    description: |-
                      provisioned is true when the infrastructure provider reports that the Machine's infrastructure is fully provisioned.
                      NOTE: this field is part of the Cluster API contract, and it is used to orchestrate initial Machine provisioning.
                    type: boolean
                type: object
              instanceID:
                description: instanceID is the Nebius instance ID of the machine.
                type: string
              instanceState:
                description: instanceState is the state of the Nebius instance.
                type: string
              osDiskID:
                description: osDiskID is the Nebius disk ID of the OS disk attached
                  to the instance.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
