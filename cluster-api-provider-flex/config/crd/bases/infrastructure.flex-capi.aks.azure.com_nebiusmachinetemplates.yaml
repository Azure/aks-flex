---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nebiusmachinetemplates.infrastructure.flex-capi.aks.azure.com
spec:
  group: infrastructure.flex-capi.aks.azure.com
  names:
    kind: NebiusMachineTemplate
    listKind: NebiusMachineTemplateList
    plural: nebiusmachinetemplates
    singular: nebiusmachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta2
    schema:
      openAPIV3Schema:
        description: NebiusMachineTemplate is the Schema for the nebiusmachinetemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NebiusMachineTemplate
            properties:
              template:
                properties:
                  metadata:
                    type: object
                  spec:
                    description: NebiusMachineSpec defines the desired state of NebiusMachine.
                    properties:
                      imageFamily:
                        description: |-
                          imageFamily is the Nebius image family to use for this machine (e.g. "ubuntu24.04-driverless").
                          Defaults to infer from platform and preset if not specified.
                        type: string
                      osDiskSizeGibibytes:
                        default: 100
                        description: osDiskSizeGibibytes is the size of the OS disk
                          in GiB.
                        format: int32
                        minimum: 1
                        type: integer
                      platform:
                        description: platform is the Nebius platform to use for this
                          machine (e.g. "cpu-d3", "gpu-h200-sxm").
                        type: string
                      preset:
                        description: preset is the Nebius preset to use for this machine
                          (e.g. "4vcpu-16gb", "1gpu-128vcpu-1600gb").
                        type: string
                      projectID:
                        description: projectID is the Nebius project ID where the
                          instance should be created.
                        type: string
                      providerID:
                        description: providerID is the unique identifier as specified
                          by the cloud provider.
                        maxLength: 512
                        minLength: 1
                        type: string
                      region:
                        description: region is the Nebius region where the instance
                          should be created.
                        type: string
                      subnetID:
                        description: subnetID is the Nebius subnet ID to attach the
                          instance to.
                        type: string
                    required:
                    - platform
                    - preset
                    - projectID
                    - region
                    - subnetID
                    type: object
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
