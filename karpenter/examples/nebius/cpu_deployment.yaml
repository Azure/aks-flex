apiVersion: apps/v1
kind: Deployment
metadata:
  name: nebius-sample-cpu-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nebius-sample-cpu-app
  template:
    metadata:
      labels:
        app: nebius-sample-cpu-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodepool
                    operator: In
                    values:
                      - nebius-cpu-nodepool
      containers:
        - name: app-container
          image: nginx:1.21
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"