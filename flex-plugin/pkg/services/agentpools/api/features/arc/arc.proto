edition = "2024";

package arc;

option go_package = "github.com/Azure/aks-flex/flex-plugin/pkg/services/agentpools/api/features/arc";

// None of this is implemented at this time; it's just an example.

message Config {
  string resource_id = 1; // /subscriptions/<subscription>/resourceGroups/<resourceGroup>

  string tenant_id                = 2;
  string service_principal_id     = 3;
  string service_principal_secret = 4;

  // Later options: service principal cert; ARC private link; ARC gateway
}
