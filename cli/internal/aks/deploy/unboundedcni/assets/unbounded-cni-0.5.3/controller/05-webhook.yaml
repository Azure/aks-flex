---
apiVersion: v1
kind: Service
metadata:
  name: unbounded-cni-webhook
  namespace: unbounded-cni
  labels:
    app.kubernetes.io/name: unbounded-cni-controller
    app.kubernetes.io/component: controller
spec:
  selector:
    app.kubernetes.io/name: unbounded-cni-controller
  ports:
    - name: https
      port: 9443
      targetPort: webhook
      protocol: TCP
