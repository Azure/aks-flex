---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: sitegatewaypoolassignments.unbounded.aks.azure.com
spec:
  group: unbounded.aks.azure.com
  names:
    kind: SiteGatewayPoolAssignment
    listKind: SiteGatewayPoolAssignmentList
    plural: sitegatewaypoolassignments
    shortNames:
    - sgpa
    singular: sitegatewaypoolassignment
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SiteGatewayPoolAssignment links sites to gateway pools.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiteGatewayPoolAssignmentSpec defines desired state for site
              to gateway-pool assignments.
            properties:
              bfdSettings:
                description: BFDSettings controls BFD for this site-to-pool assignment.
                properties:
                  detectMultiplier:
                    description: |-
                      DetectMultiplier is the multiplier used to determine session down detection.
                      Valid range is 1-255.
                    format: int32
                    type: integer
                  enabled:
                    description: Enabled controls whether BFD is enabled for this
                      scope.
                    type: boolean
                  receiveInterval:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      ReceiveInterval is the minimum interval between received BFD packets.
                      Accepts either a duration string (e.g. "300ms") or an integer interpreted as milliseconds.
                    x-kubernetes-int-or-string: true
                  transmitInterval:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      TransmitInterval is the minimum interval between transmitted BFD packets.
                      Accepts either a duration string (e.g. "300ms") or an integer interpreted as milliseconds.
                    x-kubernetes-int-or-string: true
                type: object
              enabled:
                description: |-
                  Enabled controls whether this assignment is active.
                  Defaults to true.
                type: boolean
              gatewayPools:
                description: GatewayPools is the list of gateway pool names.
                items:
                  type: string
                type: array
              sites:
                description: Sites is the list of site names.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
